# IMPORT_REPORT.md

This import report is generated by the LedgerMate CSV importer. It lists every anomaly detected during ingestion of `expenses_export.csv` and the automated or manual action taken for each.

Generated on: 2026-06-14

Summary:
- Rows scanned:  fifty-some (source CSV)
- Anomalies detected: 13 distinct types (see details below)

Detailed Anomalies & Actions

1. Date overflow (`########`) — Rows: 7,8,21-26,39-40
   - Detection: Cells containing `#` sequences.
   - Action: Interpolated dates from neighboring rows; flagged for user review in staging sandbox.

2. Inconsistent date formats
   - Detection: Mixed formats like `1/2/2026`, `14-02-2026`, `14-Mar`, `15-04-202`.
   - Action: Normalized to ISO `YYYY-MM-DD`. `14-Mar` assumed 2026; `15-04-202` expanded to `2026-04-15`.

3. Ambiguous date `4/5/2026`
   - Detection: Position between Mar 28 and Apr 1.
   - Action: Parsed as `2026-04-05` using contextual inference; warning logged.

4. Payer name typos/case issues
   - Detection: `priya`, `Priya S` etc.
   - Action: Normalized to canonical member names using case-insensitive lookups and typo map.

5. Missing payer
   - Detection: Empty `paid_by` in row 13.
   - Action: Marked `u_unknown`; required manual selection before import.

6. Missing currency
   - Detection: Row 28 missing currency field.
   - Action: Defaulted to `INR` and flagged.

7. Zero amount
   - Detection: Amount == 0 in row 31.
   - Action: Excluded from import by default; user may edit amount in sandbox.

8. Negative amount (refund)
   - Detection: Amount < 0 in row 26.
   - Action: Ingested as negative expense (refund) with corresponding negative splits.

9. Percentages sum > 100%
   - Detection: Splits total 110% in some rows.
   - Action: Rebalanced percentages proportionally to sum to 100%; warning logged.

10. Equal split with numeric share details
    - Detection: Equal split type with explicit shares provided.
    - Action: Normalized to equal shares and flagged for confirmation.

11. Settlements logged as expenses
    - Detection: Description keywords and single-member splits.
    - Action: Marked `is_settlement=true` and excluded from shared split calculations.

12. Duplicate entries
    - Detection: Exact and fuzzy duplicate detection on payer/date/amount/description.
    - Action: Both duplicates staged; user prompted to choose which to import.

13. Temporal membership violations
    - Detection: Expenses including members outside their membership windows.
    - Action: Automatically excluded inactive members and recalculated splits among active members.

All flagged rows are available in the Staging Sandbox UI for manual review. Approved rows were written to PostgreSQL with `imported_at` timestamps.
